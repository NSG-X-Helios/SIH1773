{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:scope",
    "fs:allow-resource-read-recursive",
    "fs:allow-appdata-write-recursive",
    "dialog:default",
    "shell:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "args": [
            {
              "validator": ".+"
            },
            "--output",
            {
              "validator": ".+"
            },
            "--wall_texture",
            {
              "validator": ".+"
            },
            "--floor_texture",
            {
              "validator": ".+"
            },
            "--scale_factor",
            {
              "validator": ".+"
            },
            "--wall_height",
            {
              "validator": ".+"
            }
          ],
          "name": "binaries/convertor",
          "sidecar": true
        }
      ]
    }
  ]
}
